#include "main.h"
#include <stdio.h>

/**
 * mul - multiplica los numeros a y b
 * @a: first paramet
 * @b: second paramet
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int resu;

	resu = a * b;
	return (resu);
}

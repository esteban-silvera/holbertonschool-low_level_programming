#include "main.h"
/**
 *_islower - verific is lower or not 
 *
 * Return: Returns 1 if c is lower or Returns 0 is not lower 
 */
	int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
{
	return (1);
}
	return (0);
}

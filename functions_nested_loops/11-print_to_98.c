#include "main.h"
#include <stdio.h>
/**
 *
 *Return: 
 */

void print_to_98(int n)
{
	int nu;

	for (nu = n; nu <= 98; nu++)
	{
		printf("%d", nu)
		if (nu < 98)
		{

			printf(",")
			printf(" ")
		}
	}
		if (n > 98)
		{
			for (nu = n; nu >= 98; nu--)
				{
					printf("%d")
						if (nu > 98)
						{
							printf(",")
							printf(" ")
						}

				}
		}
		printf("\n")
}

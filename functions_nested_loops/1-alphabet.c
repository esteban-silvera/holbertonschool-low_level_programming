#include "main.h"
#include <stdio.h>

/**
 * print_alphabet - pritn function
 *
 * Return: Always 0 (Success)
 */

void print_alphabet(void)
{
	char x;

	for (x = 'a' ; x <= 'z'; x++)
	{
		_putchar(x);
	} _putchar('\n');
}

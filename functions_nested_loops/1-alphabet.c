#include "main.h"
#include <stdio.h>
/**
 *print_alphabet- Entry point
 *Return: Always 0 (Success)
 */
int  main(void)
{
	char x;

	for (x = 'a' ; x <= 'z'; x++)
	{
		_putchar(x);
	}
	_putchar('\n');
}

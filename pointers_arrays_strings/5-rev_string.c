#include "main.h"
/**
 *print_rev - function print a text reverse
 *@s: the string
 */

void rev_string(char *s)
{
	int x;
	int n = 0;
	char rs;

	while (s[n] != '\0')
	{
		n++;
	}

	for (x = n - 1; x >= 0; x--)
	{
		ss = s[x];
	}
}

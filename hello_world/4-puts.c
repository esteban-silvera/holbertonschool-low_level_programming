#include <stdio.h>

/*
 *main - Prints "Programming is like building a multilingual
 *                puzzle, followed by a new line."
 *                return: always 0 
 */

int main(void)
{
	puts("\"Programing is like building a multilingua1 puzzle")

		return (0);
}
